<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.foo.mappers.BusinessMapper">



<!-- 사업자 회원가입 -->
<insert id="insertBusinessuser">
	INSERT INTO businessusers (buId, buPw, buNum, buName, buEmail, buTel, regdate) 
	VALUES (#{buId}, #{buPw}, #{buNum}, #{buName}, #{buEmail}, #{buTel}, now())
</insert>
<!-- 사업자 회원가입 -->



<!-- 사업자 등록 -->
<insert id="insertRestaurant">
	INSERT INTO restaurants (restId, restName, restTel, restAddr, restDescription, restRuntime, restCategory, restConvenience, restExterior, restInterior) 
	VALUES (#{restId}, #{restName}, #{restTel}, #{restAddr}, #{restDescription}, #{restRuntime}, #{restCategory}, #{restConvenience}, #{restExterior}, #{restInterior})
</insert>
<!-- 사업자 등록 -->



<!-- 마이페이지 - 가게 정보 (입점 신청이 완료된 경우) -->
<select id="readMyRestaurantInfo" resultType="com.foo.domain.RestaurantsVO">
	SELECT * FROM restaurants 
	WHERE restId = #{restId}
</select>
<!-- 마이페이지 - 가게 정보 -->



</mapper>